#+title: Notes
* Send
- Subject
- Message
- Sender
  - Who e.g. Script instance
  - Where e.g. Statement instance (line)

* Message
- Unary
- Binary
- Keyword
- ArgumentList

* Message interning

** Unary
not = not

** Arity
list(*elems)  = list__1 # arity = -1
cons(x, list) = cons_2 # arity = 2
first(list)   = first_1  # arity = 1

** Binary
1 + 2 = bin_+
true and false = bin_and

** Keyword
if: { } else: { } = kw_if_else

* References
https://en.wikipedia.org/wiki/Virtual_method_table
